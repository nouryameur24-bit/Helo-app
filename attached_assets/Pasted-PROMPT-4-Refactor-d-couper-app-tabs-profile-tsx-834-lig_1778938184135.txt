PROMPT 4 — Refactor : découper app/(tabs)/profile.tsx (834 lignes → < 250)
[CONTEXTE]
app/(tabs)/profile.tsx fait 834 lignes en un seul composant ProfileScreen.
C'est devenu illisible. À découper SANS changer le comportement (refactor pur).

[OBJECTIF]
Réduire app/(tabs)/profile.tsx à moins de 250 lignes en extrayant les blocs
logiques dans components/profile/.

[ÉTAPES]

1. Identifier les blocs logiques dans le fichier actuel (ils sont déjà
   visuellement séparés par les sections de l'UI). Les regrouper ainsi :
   - En-tête utilisateur (nom, semaine de grossesse, GlowScore mini)
   - Section "Mon partenaire" (PartnerCodeChip, invitation, unlink)
   - Section "Statistiques" (badges PACT, journal export)
   - Section "Préférences" (notifications, baby mode, breastfeeding mode)
   - Section "Premium" (subscription status, manage)
   - Section "Légal" (privacy, terms, methodology, about)
   - Section "Compte" (regenerate code, logout, delete account)

2. Pour chaque bloc, créer un composant dédié dans components/profile/ :
   - components/profile/ProfileHeader.tsx
   - components/profile/PartnerSection.tsx
   - components/profile/StatsSection.tsx
   - components/profile/PreferencesSection.tsx
   - components/profile/PremiumSection.tsx
   - components/profile/LegalSection.tsx
   - components/profile/AccountActions.tsx

3. Règles pour chaque sous-composant :
   - Pas de props "drilling" inutile : si le sous-composant a besoin du profil,
     il appelle `useProfile()` lui-même.
   - Idem pour useNotifications, usePremium, useBabyMode, useBreastfeeding.
   - Les callbacks lourds (regenerate code, unlink partner, export PDF, logout)
     restent dans le sous-composant qui en a l'usage exclusif.
   - Garder les styles dans components/profile/profileStyles.ts (déjà existant) ;
     ajouter les nouveaux styles à ce fichier, ne PAS créer un styles par
     sous-composant.

4. app/(tabs)/profile.tsx final doit ressembler à :
export default function ProfileScreen() {
const insets = useSafeAreaInsets();
const topPadding = Platform.OS === 'web' ? 67 : insets.top;
const bottomPadding = Platform.OS === 'web' ? 34 : 0;
 return (
   <ScrollView contentContainerStyle={[styles.container, { paddingTop: topPadding, paddingBottom: bottomPadding }]}>
     <ProfileHeader />
     <PartnerSection />
     <StatsSection />
     <PreferencesSection />
     <PremiumSection />
     <LegalSection />
     <AccountActions />
   </ScrollView>
 );
}

[VÉRIFICATIONS]
- `wc -l app/\(tabs\)/profile.tsx` < 250.
- Aucun sous-composant ne dépasse 200 lignes (sinon, re-découper).
- `pnpm typecheck` passe.
- Test manuel : l'écran Profil rend EXACTEMENT comme avant le refactor,
  tous les boutons fonctionnent, toutes les nav fonctionnent.

[RÈGLES STRICTES]
- AUCUN changement de logique métier dans ce refactor.
- AUCUN changement de style visible.
- Si un comportement change, c'est un bug à corriger AVANT de commit.

