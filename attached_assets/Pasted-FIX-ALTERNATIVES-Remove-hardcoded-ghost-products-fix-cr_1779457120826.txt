FIX ALTERNATIVES — Remove hardcoded ghost products, fix critical issues.

PROBLEM: The hardcoded fallback in lib/alternatives.ts contains 123 
products with no barcode, no image, no composition. Users see them 
suggested but can't scan/verify them. This breaks trust on a health app.

SOLUTION — Replace hardcoded list with a smarter fallback:

1. REMOVE the HARDCODED_ALTERNATIVES object entirely from lib/alternatives.ts.

2. REPLACE the getHardcodedAlternatives function with this:

function buildEmptyStateMessage(productName: string, category: string): {
  message: string;
  cta: string;
} {
  return {
    message: `Nous n'avons pas encore d'alternative validée pour ce 
      type de produit dans notre base. Pour t'aider à choisir, voici 
      les critères à vérifier sur les autres produits que tu trouves :`,
    cta: 'Scanner d\'autres produits →',
  };
}

3. UPDATE app/alternatives.tsx empty state:
When no alternatives are found, show an EDUCATIONAL screen instead:

"📋 Pas d'alternative dans notre base pour cette catégorie

En attendant que la communauté Hēlo enrichisse la base, voici les 
ingrédients à ÉVITER lors de ton prochain scan :

[List of flagged ingredients from current product with 1-line CRAT 
explanation each]

🔍 Scanne un autre produit pour comparer
🤍 Tu enrichis automatiquement la base à chaque scan"

This is HONEST. We don't invent products. We educate.

4. FIX premium gating bug — when fewer than 3 real alternatives exist:

In app/alternatives.tsx, if alternatives.length === 1 AND user is not 
Premium, show 2 additional "ghost cards" with:
- Blurred background
- Lock icon
- Text: "Plus d'alternatives disponibles en Premium"
- Subtext: "Découvre des produits validés selon ton trimestre"

This way free users always see the value of Premium, even when the 
algorithm returns few results.

5. FIX matching strictness — distinguish danger from caution:

In filterSafeProducts, accept the flagged ingredients as a categorized object:
{
  danger: ['rétinol', 'isotrétinoïne'],
  caution: ['parfum', 'alcool denat']
}

Filter out products containing DANGER ingredients (hard exclusion).
Keep products containing only CAUTION ingredients but penalize them 
in sort order.

This prevents excluding