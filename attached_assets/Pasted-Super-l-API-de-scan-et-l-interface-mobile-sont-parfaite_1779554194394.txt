Super, l'API de scan et l'interface mobile sont parfaitement en place. Nous allons maintenant coder la suite logique du backend : le "Moteur des Alternatives" avec l'architecture du "Filet et du Sniper".

[MISSION]
Créer une fonction backend robuste `findAlternatives(originalBarcode, trimester, searchKeyword)` dans `artifacts/api-server/src/routes/scan.ts` (ou un service dédié).
Cette fonction sera appelée par la route `/api/scan` UNIQUEMENT lorsque le verdict d'un produit scanné est "a_eviter" ou "interdit".

[ALGORITHME STRICT EN 4 ÉTAPES]

1. CACHE HIT (Alternatives) :
- Vérifie si le produit d'origine possède déjà des alternatives en cache pour ce trimestre dans Supabase (ex: dans `analysis_cache[trimester].alternatives`).
- Si OUI -> Retourne immédiatement ces codes-barres. Terminé.

2. LE FILET (Requête Supabase) :
- Si Cache Miss, fais une requête ultra-rapide à Supabase sur la table `products`.
- Cherche 20 produits (LIMIT 20) dont le `name` ou la catégorie ressemble au `searchKeyword` (généré par Claude ou notre base). Utilise `ILIKE` ou une recherche textuelle.
- Exclus le produit d'origine (`barcode != originalBarcode`).
- Récupère uniquement les colonnes utiles : `barcode`, `name`, `ingredients_raw`.

3. LE SNIPER & LA TRAPPE DE SECOURS (Appel Claude 3.5 Haiku) :
- Envoie la liste des 20 candidats (avec leurs ingrédients) à l'API Anthropic.
- Utilise EXACTEMENT ce System Prompt :
"Tu es un expert en toxicologie périnatale. Voici 20 produits avec leurs listes d'ingrédients. L'utilisatrice est au trimestre {trimester} de sa grossesse.
Ta mission : Sélectionne jusqu'à 3 produits qui sont 100% SANS DANGER (zéro composant toxique, zéro risque selon les recommandations médicales).
🚨 SÉCURITÉ CRITIQUE (Trappe de Secours) : Si AUCUN produit de cette liste n'est parfaitement sûr pour ce trimestre, tu DOIS IMPÉRATIVEMENT renvoyer un tableau JSON vide []. Ne fais aucun compromis. Ne choisis pas 'le moins pire'.
Tu dois répondre UNIQUEMENT par un tableau JSON contenant les codes-barres des produits validés, ou un tableau vide. Exemple : ['123456789', '987654321']"

4. LA CONSOLIDATION (Mise à jour du Cache et de la Réponse) :
- Récupère le tableau JSON renvoyé par Claude (qu'il contienne 3 produits, 1 produit, ou qu'il soit vide `[]`).
- Sauvegarde ce tableau dans le cache du produit d'origine sur Supabase (`analysis_cache[trimester].alternatives = result`).
- Modifie la réponse de la route `/api/scan` pour qu'elle renvoie ce tableau `alternatives` au frontend.

[CONTRAINTES DE CODE]
- Utilise Claude 3.5 Haiku (le plus rapide et économique pour cette tâche de tri textuel).
- Gère parfaitement les erreurs (si Claude renvoie un JSON mal formaté, catch l'erreur et retourne un tableau vide par défaut).

Affiche-moi le code de ce moteur d'alternatives pour que je valide la logique.