Final perfection pass — last 0.5% to reach 10/10:

1. SPLIT REMAINING LARGE FILES (all files over 800 lines):
- app/scan-party.tsx (982 lines) → extract ScanPartyCamera, ScanPartyResults, ScanPartyExport into components/scan-party/
- app/ar-mirror.tsx (976 lines) → extract ARCameraView, AROverlayLayer, ARCaptureSheet into components/ar-mirror/
- app/circle.tsx (921 lines) → extract CircleFeed, CircleMembers, CircleInvite into components/circle/
- app/compare.tsx (915 lines) → extract CompareColumn, CompareVerdict, CompareShareView into components/compare/
- app/(tabs)/scan.tsx (887 lines) → extract ScanViewfinder, ScanModeChips, ScanPermission into components/scanner/
- app/voice.tsx (870 lines) → extract VoiceRecorder, VoiceResponse, VoiceHistory into components/voice/
- app/nutrition.tsx (860 lines) → extract NutritionNeeds, NutritionRecipes, NutritionTips into components/nutrition/
- app/search.tsx (813 lines) → extract SearchBar, SearchResults, BrowseCategories into components/search/
- Max 400 lines per file. Each sub-component gets its own file.

2. STRICT LINT RULES — Create .eslintrc.js:
{
  extends: ['expo', 'plugin:@typescript-eslint/strict'],
  rules: {
    '@typescript-eslint/no-explicit-any': 'error',
    '@typescript-eslint/no-unused-vars': 'error',
    'no-console': 'error',
    'react/jsx-no-bind': 'warn',
    'max-lines': ['warn', { max: 400 }],
  }
}

3. ADD PERFORMANCE GUARDS — In every FlatList in the app:
- Add getItemLayout where possible
- Add maxToRenderPerBatch={10}
- Add windowSize={5}
- Wrap every renderItem component in React.memo
- Add keyExtractor using unique IDs

4. FINAL TEST COVERAGE — Add:
- __tests__/circleUtils.test.ts (test invite code, join/leave)
- __tests__/travel.test.ts (test briefing generation)
- __tests__/voiceLimit.test.ts (test daily limit)
- __tests__/scanLimit.test.ts (test 5/day counter reset)
- __tests__/offline.test.ts (test cache, sync)
Target: 15 test suites minimum

5. Verify everything:
- npx tsc --noEmit → zero errors
- All imports resolve
- No circular dependencies