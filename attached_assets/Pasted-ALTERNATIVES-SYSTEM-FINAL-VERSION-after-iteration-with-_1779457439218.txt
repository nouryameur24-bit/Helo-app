ALTERNATIVES SYSTEM — FINAL VERSION (after iteration with engineering review).

Decisions:
- Quality sorting applies to EVERYONE (free + premium)
- Premium adds: visual quality badges, ingredient-by-ingredient CRAT 
  explanations, trimester-specific filtering, and access to 5 vs 3 results
- NO notifications/recall feature (out of scope for V1)
- NO fake/hardcoded products
- NO ghost premium teaser cards

═══════════════════════════════════════════════════════════
PART 1 — DELETE FAKE PRODUCTS
═══════════════════════════════════════════════════════════

In lib/alternatives.ts:
- DELETE the HARDCODED_ALTERNATIVES object
- DELETE the getHardcodedAlternatives function

═══════════════════════════════════════════════════════════
PART 2 — SMART MATCHING (3 layers) + QUALITY SORTING FOR ALL
═══════════════════════════════════════════════════════════

export async function getAlternativesByBarcode(
  barcode: string,
  flaggedIngredients: { danger: string[]; caution: string[] },
  trimester: Trimester,
  isPremium: boolean,
): Promise<AlternativeProduct[]> {
  if (!isSupabaseConfigured || !barcode) return [];
  
  const { data: current } = await supabase
    .from('products')
    .select('id, name, brand, category, ingredients_raw')
    .eq('barcode', barcode)
    .maybeSingle();
  
  if (!current) return [];
  
  const productKeywords = extractProductKeywords(current.name);
  const limit = isPremium ? 5 : 3;
  
  // LAYER 1: Keyword match in name
  if (productKeywords.length > 0) {
    const orFilter = productKeywords.map(kw => `name.ilike.%${kw}%`).join(',');
    const { data: candidates } = await supabase
      .from('products')
      .select('id, name, brand, category, barcode, image_url, ingredients_raw')
      .eq('category', current.category)
      .neq('id', current.id)
      .not('ingredients_raw', 'is', null)
      .or(orFilter)
      .limit(30);
    
    const filtered = filterAndScore(candidates || [], flaggedIngredients, trimester);
    if (filtered.length >= limit) return filtered.slice(0, limit);
    
    // LAYER 2: Same brand fallback
    if (current.brand) {
      const { data: brandProducts } = await supabase
        .from('products')
        .select('id, name, brand, category, barcode, image_url, ingredients_raw')
        .eq('brand', current.brand)
        .neq('id', current.id)
        .not('ingredients_raw', 'is', null)
        .limit(20);
      
      const brandSafe = filterAndScore(brandProducts || [], flaggedIngredients, trimester);
      const combined = mergeUnique(filtered, brandSafe);
      if (combined.length >= limit) return combined.slice(0, limit);
      if (combined.length > 0) return combined;
    }
    
    if (filtered.length > 0) return filtered;
  }
  
  // LAYER 3: Same category, any safe product
  const { data: anyCat } = await supabase
    .from('products')
    .select('id, name, brand, category, barcode, image_url, ingredients_raw')
    .eq('category', current.category)
    .neq('id', current.id)
    .not('ingredients_raw', 'is', null)
    .limit(50);
  
  return filterAndScore(anyCat || [], flaggedIngredients, trimester).slice(0, limit);
}

═══════════════════════════════════════════════════════════
PART 3 — FILTERING & SCORING (quality bonus for everyone)
═══════════════════════════════════════════════════════════

function filterAndScore(
  candidates: any[],
  flagged: { danger: string[]; caution: string[] },
  trimester: Trimester,
): AlternativeProduct[] {
  const dangerLower = flagged.danger.map(n => n.toLowerCase());
  const cautionLower = flagged.caution.map(n => n.toLowerCase());
  
  // Trimester-specific extra exclusions (uses our existing ingredient data)
  const TRIMESTER_EXTRA_EXCLUSIONS: Record<Trimester, string[]> = {
    T1: ['alcool denat', 'alcohol denat', 'éthanol', 'ethanol', 'caféine', 'caffeine'],
    T2: [],
    T3: ['ibuprofène', 'ibuprofen', 'aspirine', 'aspirin', 'diclofenac', 'naproxen'],
  };
  
  const extraExclusions = TRIMESTER_EXTRA_EXCLUSIONS[trimester] || [];
  
  return candidates
    .map(c => {
      const ingredientsLower = (c.ingredients_raw || '').toLowerCase();
      
      // HARD EXCLUSION: danger ingredients
      if (dangerLower.some(d => ingredientsLower.includes(d))) return null;
      
      // HARD EXCLUSION: trimester-specific risks
      if (extraExclusions.some(e => ingredientsLower.includes(e))) return null;
      
      // SOFT PENALTY: caution ingredients
      const cautionCount = cautionLower.filter(c => ingredientsLower.includes(c)).length;
      
      // QUALITY BONUS (applies to EVERYONE, not just Premium)
      const brand = (c.brand || '').toLowerCase();
      const PHARMACY_BRANDS = ['avène', 'avene', 'la roche-posay', 'roche posay',
        'mustela', 'bioderma', 'uriage', 'a-derma', 'aderma', 'klorane',
        'weleda', 'cattier', 'cetaphil', 'cerave', 'eucerin', 'ducray', 'nuxe'];
      const FRENCH_BRANDS = ['caudalie', 'embryolisse', 'lierac', 'sanoflore',
        'melvita', 'galenic', 'phyto', 'rené furterer'];
      
      let bonusScore = 0;
      let originBadge: 'pharmacy' | 'french' | 'bio' | null = null;
      
      if (PHARMACY_BRANDS.some(b => brand.includes(b))) {
        bonusScore += 15;
        originBadge = 'pharmacy';
      } else if (FRENCH_BRANDS.some(b => brand.includes(b))) {
        bonusScore += 8;
        originBadge = 'french';
      }
      
      // Bio detection from name or brand
      if (ingredientsLower.includes('bio') || (c.name || '').toLowerCase().includes('bio')) {
        bonusScore += 5;
        if (!originBadge) originBadge = 'bio';
      }
      
      return {
        product: c,
        score: 100 - (cautionCount * 15) + bonusScore,
        cautionCount,
        originBadge,
      };
    })
    .filter((x): x is NonNullable<typeof x> => x !== null)
    .sort((a, b) => b.score - a.score)
    .map(x => ({
      id: x.product.id,
      name: x.product.name,
      brand: x.product.brand,
      category: x.product.category,
      barcode: x.product.barcode,
      image_url: x.product.image_url,
      description_fr: null,
      overall_risk: x.cautionCount === 0 ? 'safe' : 'caution',
      price_range: '',
      popularity_count: x.score,
      origin_badge: x.originBadge,  // NEW FIELD
    }));
}

═══════════════════════════════════════════════════════════
PART 4 — UPDATE AlternativeProduct INTERFACE
═══════════════════════════════════════════════════════════

export interface AlternativeProduct {
  id: string;
  name: string;
  brand: string;
  category: Category;
  barcode: string | null;
  image_url: string | null;
  description_fr: string | null;
  overall_risk: 'safe' | 'caution';
  price_range: string;
  popularity_count: number;
  origin_badge: 'pharmacy' | 'french' | 'bio' | null;
}

═══════════════════════════════════════════════════════════
PART 5 — PRODUCT KEYWORDS
═══════════════════════════════════════════════════════════

function extractProductKeywords(name: string): string[] {
  const lower = name.toLowerCase();
  const PRODUCT_TYPES = [
    // Cosmetics
    'crème visage', 'crème mains', 'crème corps', 'crème solaire',
    'crème nuit', 'crème jour', 'crème anti-âge', 'crème hydratante',
    'lait corps', 'lait visage', 'lait démaquillant', 'lait hydratant',
    'shampoing', 'après-shampoing', 'masque cheveux', 'huile cheveux',
    'gel douche', 'savon', 'pain de toilette', 'déodorant', 'parfum',
    'eau micellaire', 'eau florale', 'lotion tonique', 'tonique',
    'dentifrice', 'bain de bouche', 'fond de teint', 'mascara',
    'rouge à lèvres', 'baume lèvres', 'gloss', 'crayon', 'fard',
    'huile démaquillante', 'sérum', 'masque visage', 'gommage',
    'huile végétale', 'beurre karité', 'liniment', 'cold cream',
    'soin', 'baume',
    // Food
    'yaourt', 'fromage', 'jambon', 'saumon', 'thon', 'chocolat',
    'biscuit', 'pâte à tartiner', 'jus', 'eau', 'lait', 'beurre',
    'huile olive', 'huile colza', 'pâtes', 'riz', 'céréales',
    'soupe', 'compote', 'pain', 'viennoiserie', 'confiture',
    'miel', 'café', 'thé', 'tisane', 'soda', 'limonade',
  ];
  
  return PRODUCT_TYPES.filter(type => lower.includes(type));
}

function mergeUnique(a: AlternativeProduct[], b: AlternativeProduct[]): AlternativeProduct[] {
  const seen = new Set(a.map(p => p.id));
  return [...a, ...b.filter(p => !seen.has(p.id))];
}

═══════════════════════════════════════════════════════════
PART 6 — UPDATE app/alternatives.tsx
═══════════════════════════════════════════════════════════

A. Receive params from verdict screen:
   - barcode, productName, productBrand
   - flaggedDanger (JSON array)
   - flaggedCaution (JSON array)
   - trimester (T1/T2/T3)

B. Pass isPremium from usePremium() hook to getAlternativesByBarcode.

C. EMPTY STATE (alternatives.length === 0):

"📋 Pas d'alternative dans notre base pour ce type de produit

Pour t'aider à choisir, voici les ingrédients à éviter sur les 
autres produits de cette catégorie :

[For each ingredient in flagged.danger, show a red card with:
 - Ingredient name
 - One-line CRAT explanation (use existing ingredient data from DB)]

[For each ingredient in flagged.caution, show an amber card]

Scanne d'autres produits — tu enrichis la communauté à chaque scan 🤍

[CTA: Continuer à scanner →]"

D. NORMAL STATE (alternatives.length >= 1):

Each alternative card displays:
  - Product image (image_url or placeholder)
  - Product name + brand
  - Origin badge (visible to ALL users):
      'pharmacy' → 💊 Pharmacie (golden border)
      'french'   → 🇫🇷 Marque française
      'bio'      → 🌿 Bio
      null       → no badge
  - For FREE users: just the badge, no detailed explanation
  - For PREMIUM users: 
      - The badge
      - A 2-3 line explanation: "Cette alternative est compatible 
        car elle ne contient ni [ingredient1] ni [ingredient2]. 
        Selon le CRAT, ses ingrédients actifs sont validés pour 
        votre trimestre."
      - Tap "Détails" → opens full CRAT-sourced breakdown of the 
        product's ingredients
  - "Voir →" button → opens that product's verdict screen

E. PREMIUM UPGRADE CARD (only shown to free users, below results):

If isPremium === false:
  Display a Premium card with ONLY these 3 honest benefits:
  
  "💎 Avec Premium :
  
  ✓ Explication détaillée par ingrédient (sources CRAT)
  ✓ Filtrage adapté à ton trimestre actuel ({T1/T2/T3})
  ✓ Jusqu'à 5 alternatives au lieu de 3
  
  [Essayer 7 jours gratuit →]"
  
  NO mention of recall notifications. NO false promises.

═══════════════════════════════════════════════════════════
PART 7 — VERDICT SCREEN INTEGRATION
═══════════════════════════════════════════════════════════

In components/verdict/VerdictBottomBar.tsx:

- "Voir les alternatives →" button only shown if score < 100
- Pass to router.push:
  {
    pathname: '/alternatives',
    params: {
      barcode: scan.barcode,
      productName: scan.productName,
      productBrand: scan.brand,
      flaggedDanger: JSON.stringify(verdict.dangerIngredients.map(i => i.name)),
      flaggedCaution: JSON.stringify(verdict.cautionIngredients.map(i => i.name)),
      trimester: userProfile.trimester,
    }
  }

═══════════════════════════════════════════════════════════
PART 8 — VERIFICATION
═══════════════════════════════════════════════════════════

After implementation:
1. npx tsc --noEmit → 0 errors
2. Test "Crème visage Nivea Q10" (rétinol flagged) → real crème visage 
   alternatives, Avène/Bioderma at top with 💊 badge
3. Test "Yaourt aux fruits Danone" (additif flagged) → real yaourts 
   without that additive
4. Test obscure product with 0 results → educational empty state shows 
   with ingredient breakdown, NO crash, NO ghost products
5. Test as Free user → 3 alternatives + cards have origin badges + 
   honest Premium card with only 3 real benefits
6. Test as Premium user → 5 alternatives + detailed CRAT explanations 
   per card
7. Test trimester filter: scan a product as T1 user vs T3 user → 
   alternatives must differ (T1 excludes alcohol/caffeine, T3 excludes AINS)

CONFIRM what was changed and show 2-3 sample outputs.